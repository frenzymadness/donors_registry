on:
  push:
    branches:
    - master
  pull_request:
    branches:
    - master

name: Run Tox tests

jobs:
  tox_test:
    name: Tox test
    steps:
    - uses: actions/checkout@v2
    - name: Run Tox tests
      id: test
      uses: frenzymadness/tox-github-action@workdir
      with:
        tox_env: ${{ matrix.tox_env }}
        dnf_install: pkgconfig(libffi) glibc-langpack-cs libicu-devel sqlite-devel
        workdir: "migrations"
    strategy:
      matrix:
        tox_env: [py37, py38, py39, py310, lint]

    # Use GitHub's Linux Docker host
    runs-on: ubuntu-latest
